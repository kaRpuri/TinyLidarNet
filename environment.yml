name: tln
channels:
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - python=3.10
  - tensorflow==2.8.0[build=*cpu*]
prefix: /home/kabir/anaconda3/envs/tln
